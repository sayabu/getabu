The brand mark. Tile radius is always 32% of the side, so it scales as a squircle.

```jsx
<LogoMark />
<LogoMark size={56} wordmark={false} />
```

No other logo exists — there is no supplied logo file, so the mark is type in a tile.