Email capture. The `promise` prop is mandatory — the brand never asks for an address without saying what it sends.

```jsx
<EmailCapture title="Send me the PXPI pack"
  promise="Canvas, checklist and templates. One email, no drip campaign." />
```